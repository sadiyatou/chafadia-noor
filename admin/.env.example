# Backend API base URL, including the /api prefix.
# Local dev (backend running on the same machine): http://localhost:5000/api
# Production (backend deployed on Render, see ../render.yaml): your deployed URL
# + /api, e.g. https://chafadia-noor-api.onrender.com/api
VITE_API_URL=http://localhost:5000/api
